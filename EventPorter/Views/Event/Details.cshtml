@model EventPorter.Models.Event
@{
    ViewBag.Title = "Event";
}

@section Styles {
    <link href="@Url.Content("~/Content/styles/Event.css")" rel="stylesheet" type="text/css" />
    <link href="https://fonts.googleapis.com/css?family=Gloria+Hallelujah|Oswald|Shadows+Into+Light" rel="stylesheet">
}

@*
    if statement ( or section thing ) to see if there are any images in the model
    in the first place, otherwise no gallery
    @if(galleryImages != null && !galleryImages.IsEmpty())

    START OF IMAGE CAROUSEL
*@

@*If the event has any gallery images, display them in a carousel*@
@if (Model.Gallery != null && Model.Gallery.Count > 0)
{
    <div class="container-fluid remove-padding-left-right gallery-shadow">
        <div id="event_gallery" class="carousel slide" data-ride="carousel">
            @*
                navigation circle things down the bottom
            *@
            @*<ol class="carousel-indicators">*@
                @*
                    need to loop for each of the N images
                    <li data-target="#event_gallery" data-slide-to="0" class="active"></li>
                    @for(int i = 1; i < galleryImages.Size(); i++){
                        <li data-target="#event_gallery" data-slide-to="@i"></li>
                    }
                *@
                @*<li data-target="#event_gallery" data-slide-to="0" class="active"></li>
                <li data-target="#event_gallery" data-slide-to="1"></li>
                <li data-target="#event_gallery" data-slide-to="2"></li>
            </ol>*@

            @*
                images
            *@
            <div class="carousel-inner">
                @*
                    need to loop, adding each image in the passed model
                    <div class="item active">
                        <img src="image at index 0 in the list" alt="image name / desc / event title">
                        <div class="carousel-caption">
                            both of these could be optional, just set empty or null
                            <h3>image caption</h3>
                            <p>image text</p>
                        </div>
                    </div>
                *@
                <div class="item active">
                    <img src=@Url.Content(Model.Gallery[0].FilePath) alt="Event Gallery Image" >
                    @*
                        Optional Image text
                    *@
                    @*<div class="carousel-caption">
                        <h3>Image 1 Caption</h3>
                        <p>Some text</p>
                    </div>*@
                </div>

                @for (int i = 1; i < Model.Gallery.Count; i++)
                {
                    <div class="item">
                        <img src=@Url.Content(Model.Gallery[i].FilePath) alt="Event Gallery Image">
                        @*<div class="carousel-caption">
                                <h3>Image 2 Caption</h3>
                                <p>Some text</p>
                            </div>*@
                    </div>

                }

                @*<div class="item">
                    <img src="~/Content/images/test_img3.jpg" alt="lil P 3">
                    @*<div class="carousel-caption">
                        <h3>Image 3 Caption</h3>
                        <p>Some text</p>
                    </div>*@
                @*</div>*@
            </div>

            @*
                Left and Right arrow yokes
            *@
            @if (Model.Gallery.Count > 1)
            {
                <a class="left carousel-control" href="#event_gallery" data-slide="prev">
                    <span class="glyphicon glyphicon-chevron-left"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="right carousel-control" href="#event_gallery" data-slide="next">
                    <span class="glyphicon glyphicon-chevron-right"></span>
                    <span class="sr-only">Next</span>
                </a>
            }
        </div>
    </div>
}
@*
    END OF IMAGE CAROUSEL
*@

@*
    Start of JUMBOTRON ( PAGE TITLE / HEADER )
*@
<div class="container">
    <div id="event-title" class="jumbotron">
        @*
            <h1>@Model.Title</h1>
        *@
        <h1>@Model.Title</h1>
        @*
            <p>@Model.Description</p>
        *@
        @*<p>@Model.Description</p>*@
    </div>
</div>
@*
    END of page Header / Title
*@
<hr />

<div id="event-content" class="container">
    <div class="row col-lg-12">
        <div class="col-lg-2">
            <div id="event_left_col">
                <div id="event-owner" class="row col-xs-12">
                    <div class="row text-center">
                        <h5>
                            @Model.CreatorUserName
                        </h5>
                    </div>
                    <div class="row text-center">                               
                        @*
                            Model.Creator.Picture in here Model.Nickname for alt tag
                        *@
                        <img class="img-thumbnail" src="~/Content/images/lil_porter.png" alt="icon_image" />
                    </div>
                </div>
                @*<div class="row col-lg-12">
                    <div class="col-lg-8 text-center">
                        <div class="row">
                            <h4>Attending</h4>
                        </div>
                        <div class="row">
                            <div class="list-group">
                                <div class="list-group-item">
                                    <img src="~/Content/images/lil_porter.png" class="event_attendee_pic img-thumbnail img-rounded" />
                                </div>
                                <div class="list-group-item">
                                    <img src="~/Content/images/lil_porter.png" class="event_attendee_pic img-thumbnail img-rounded" />
                                </div>
                                <div class="list-group-item">
                                    <img src="~/Content/images/lil_porter.png" class="event_attendee_pic img-thumbnail img-rounded" />
                                </div>
                                <div class="list-group-item">
                                    <img src="~/Content/images/lil_porter.png" class="event_attendee_pic img-thumbnail img-rounded" />
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <h5 style="text-align:center;">View more...</h5>
                        </div>
                    </div>
                </div>*@
            </div>
        </div>
        <div class="col-lg-8">
            @*
                Loop through list of Model.details, display appropriately with if statements depending on the DetailsBlock enum DetailsDisplayStyle
                foreach(DetailsBlock details in Model.details){
                    if(details.style == DetailsDisplayStyle.Txt){
                    }
                    else if(details.style == DetailsDisplayStyle.Img){
                    }
                    else if(details.style == DetailsDisplayStyle.TxtImgLeft){
                    }
                    else{
                            //  TxtImgRight
                    }
                }
            *@

            
                
            <div class="details_block">

                @*
                    Pure Text Block
                *@
                <div class="row">
                    <h2 class="text-center">Information</h2>
                    <div id="description" class="col-md-12">
                        <p>
                            @Model.Description
                        </p>
                    </div>
                </div>

                <!--
                @*
                    Text block with img on left
                *@
                <div class="row">
                    <div class="col-md-12">
                        <img class="details_block_img_left img-rounded" src="~/Content/images/test_img.jpg" alt="img left of text" />
                        <p>Lorem Ipsum ist ein einfacher Demo-Text für die Print- und Schriftindustrie. Lorem Ipsum ist in der Industrie bereits der Standard Demo-Text seit 1500, als ein unbekannter Schriftsteller eine Hand voll Wörter nahm und diese durcheinander warf um ein Musterbuch zu erstellen. Es hat nicht nur 5 Jahrhunderte überlebt, sondern auch in Spruch in die elektronische Schriftbearbeitung geschafft (bemerke, nahezu unverändert). Bekannt wurde es 1960, mit dem erscheinen von "Letraset", welches Passagen von Lorem Ipsum enhielt, so wie Desktop Software wie "Aldus PageMaker" - ebenfalls mit Lorem Ipsum.</p>
                    </div>
                </div>

                @*
                    Text block with img on right
                *@
                <div class="row">
                    <div class="col-md-12">
                        <img class="details_block_img_right img-rounded" src="~/Content/images/test_img2.jpg" alt="img right of text" />
                        <p>Lorem Ipsum ist ein einfacher Demo-Text für die Print- und Schriftindustrie. Lorem Ipsum ist in der Industrie bereits der Standard Demo-Text seit 1500, als ein unbekannter Schriftsteller eine Hand voll Wörter nahm und diese durcheinander warf um ein Musterbuch zu erstellen. Es hat nicht nur 5 Jahrhunderte überlebt, sondern auch in Spruch in die elektronische Schriftbearbeitung geschafft (bemerke, nahezu unverändert). Bekannt wurde es 1960, mit dem erscheinen von "Letraset", welches Passagen von Lorem Ipsum enhielt, so wie Desktop Software wie "Aldus PageMaker" - ebenfalls mit Lorem Ipsum.</p>
                    </div>
                </div>

                @*
                    Just an image (perhaps could be another carousel)
                *@
                <div class="row">
                    <div class="details_block_img_full">
                        <img src="~/Content/images/test_img2.jpg" alt="big ass image" class="img-rounded" />
                    </div>
                </div>



                <div class="row">
                    <div class="col-md-12">
                        <p>
                            Lorem Ipsum ist ein einfacher Demo-Text für die Print- und Schriftindustrie. Lorem Ipsum ist in der Industrie bereits der Standard Demo-Text seit 1500, als ein unbekannter Schriftsteller eine Hand voll Wörter nahm und diese durcheinander warf um ein Musterbuch zu erstellen. Es hat nicht nur 5 Jahrhunderte überlebt, sondern auch in Spruch in die elektronische Schriftbearbeitung geschafft (bemerke, nahezu unverändert). Bekannt wurde es 1960, mit dem erscheinen von "Letraset", welches Passagen von Lorem Ipsum enhielt, so wie Desktop Software wie "Aldus PageMaker" - ebenfalls mit Lorem Ipsum.
                        </p>
                    </div>
                </div>

                    -->

            </div>
        </div>
        <div class="col-lg-2">
            <div class="row rightside-info">
                @*
                    @Model.DateAndTime.GetTime()
                *@
                @*
                    @Model.DateAndTime.GetSimpleDate()
                *@
                <p>Location </p>
                <p>@Model.LocationDesc</p>
                <p>Starting: </p>
                <p>@Model.StartDateAndTime.ToShortDateString()  @Model.StartDateAndTime.ToShortTimeString()</p>
                <p>Ending:</p>
                <p> @Model.EndDateAndTime.ToShortDateString()  @Model.EndDateAndTime.ToShortTimeString()</p>
                <p></p>
                @*
                    @Model.Cost
                    @if(Model.Cost == 0){
                        <p>Ticket Price: Free!</p>
                    }else{
                        <p>Ticket Price: @Model.Cost</p>
                    }

                *@
                @{ 
                    string price = "Free!";
                }
                @if (Model.Price != 0)
                {
                    price = "&euro; " + Model.Price.ToString();
                }
                <p>Ticket Price:@Html.Raw(price)</p>
                                    
                @if(Model.Price == 0){
                    <div class="btn btn-primary text-center">Get Ticket</div>
                }else{
                    <div class="btn btn-primary text-center">Add To Cart</div>
                }
                                    
                @*<div id="buy-button" class="btn btn-primary text-center">Add To Cart</div>*@
            </div>
        </div>
    </div>
</div>


